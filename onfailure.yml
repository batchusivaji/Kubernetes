apiVersion:	v1
kind: Pod
metadata:
  name: OnFailure
spec:
  restartPolicy: OnFailure
  containers:
    - name: nginix
      image: nginix
      args:
        - sleep
        - 10mb
---
apiVersion:	v1
kind: Pod
metadata:
  name: onfailure
spec:
  restartPolicy: OnFailure
  containers:
    - name: nginix
      image: nginix
      ports:
        - containerPort: 80




      